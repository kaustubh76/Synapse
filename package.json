{
  "name": "synapse",
  "version": "0.1.0",
  "private": true,
  "description": "Decentralized Intent Network for AI Agents with x402 Payments",
  "author": "Kaushtubh",
  "license": "MIT",
  "workspaces": [
    "apps/*",
    "packages/*",
    "providers/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "npm run dev --workspace=apps/api",
    "dev:providers": "npm run dev --workspace=providers/weather-bot & npm run dev --workspace=providers/crypto-bot & npm run dev --workspace=providers/news-bot",
    "dev:all": "concurrently \"npm run dev:api\" \"npm run dev:web\" \"npm run dev:providers\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "turbo": "^2.3.0",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "packageManager": "npm@10.2.0"
}
